############################################################
# Unified requirements for RunPod PyTorch environment
# - Torch is provided by the RunPod PyTorch image (do not pin here)
# - Versions are pinned for compatibility (Transformers/PEFT/BnB)
############################################################

# Core HF stack
transformers==4.44.2
datasets>=2.14.0,<3.0.0
accelerate>=0.27.0,<0.33.0
huggingface-hub>=0.22.0,<0.25.0
safetensors>=0.4.2,<0.5.0

# Quantization / fine-tuning
peft>=0.10.0,<0.12.0
bitsandbytes>=0.44.0,<0.47.0
trl>=0.9.0,<0.10.0
sentencepiece==0.1.99

# Logging / monitoring
wandb>=0.17.0,<0.18.0
tensorboard>=2.14.0

# Data processing & analysis
pandas==2.2.2
numpy>=1.26.0,<3.0.0
scikit-learn>=1.3.0,<1.6.0
matplotlib>=3.8.0,<3.9.0
seaborn>=0.13.0,<0.14.0
tqdm>=4.66.0
protobuf>=3.20.0

# Jupyter (optional)
jupyter>=1.0.0
ipywidgets>=8.0.0

# Project-specific (dataset generation)
openai>=1.3.0
openpyxl>=3.1.0